name: docker_wrapper
description: A Dart library around the Docker CLI.
version: 0.1.0
repository: https://github.com/josereia/docker_wrapper

environment:
  sdk: ^3.0.5

dependencies:
  path: ^1.8.3
  very_good_analysis: ^5.0.0+1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
